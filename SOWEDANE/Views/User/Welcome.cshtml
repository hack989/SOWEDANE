<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Home Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4; /* Background color for the entire page */
        }

        .center-container {
            text-align: center;
            background-color: #ffffff; /* Background color for the container */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Box shadow for a subtle elevation effect */
        }

        h2 {
            color: #333333; /* Text color for the heading */
        }
        /* === removing default button style ===*/
button {
            background-color: #4285f4; /* Button background color */
            color: #ffffff; /* Button text color */
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin: 5px;
            transition: background-color 0.3s ease; /* Smooth transition for background color */
        }

        button:hover {
            background-color: #3c79c8; /* Button background color on hover */
        }
        
    </style>
    <script>
        function redirectToSignUp() {
            window.location.href = '/User/Create'; // Adjust the URL as needed
        }

        function redirectToLogin() {
            window.location.href = '/User/Login'; // Adjust the URL as needed
        }
    </script>
</head>
<body>
    <div class="center-container">
        <h2>Welcome To Our Application</h2>
        <button onclick="redirectToSignUp()">Sign Up</button>
        <button onclick="redirectToLogin()">Login</button>
    </div>
</body>
</html>
